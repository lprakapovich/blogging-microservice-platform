spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:h2://mem:db;DB_CLODE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: test
    password: test
  kafka:
    bootstrap-servers: localhost:9092
